# A script to install arch with Systemd-boot
This script will setup ntp, formate disk, mount, create folder, pacstrap, generate fstab and install systemd boot
user is created no root user and no grub
